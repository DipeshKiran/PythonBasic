mytuple = ("a", "b", "c")
print(mytuple)

'''Tuple always contains at least one comma at the end if there is only on item
Example:

thistuple = ("apple",)
print(type(thistuple))

#Is not a tuple

thistuple = ("apple")
print(type(thistuple))

'''